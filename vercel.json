{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && next build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["hkg1"],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./prisma/schema.prisma"
}

